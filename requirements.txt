# PyTorch 및 필수 패키지
torch>=1.12.0
torchvision>=0.13.0
numpy>=1.20
Pillow>=8.0
scikit-image>=0.22          # structural_similarity(channel_axis=...) 사용

# 설정 파일(config.yaml) 로딩
PyYAML>=6.0

# 학습 및 시각화 도구
matplotlib>=3.3

# tqdm (진행률 바 표시 - 선택사항)
tqdm>=4.64

# bbox CSV 약지도 라벨 사용 시 필요 (선택사항)
pandas>=1.3

# Stable Diffusion teacher (pseudo-GT 생성)
diffusers>=0.17.0
transformers>=4.21.0
accelerate>=0.16.0
